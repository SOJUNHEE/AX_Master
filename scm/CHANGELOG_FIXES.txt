Flow SCM 수정 내역 (2026-09-15)
==============================

1. 프로젝트 종류 확인
- 현재 운영 앱은 Flask입니다: app.py
- legacy/ 폴더의 streamlit_app.py는 과거 Streamlit 보관본입니다.
- UI/디자인 파일(templates, static)은 기능 변경 없이 유지했습니다.

2. Render 배포 설정 강화
- requirements.txt를 현재 Flask 운영 앱 기준으로 정리했습니다.
- Linux/Render에서 gunicorn이 설치되도록 유지했습니다.
- render.yaml의 Start Command를 `python -m gunicorn app:app ...` 형식으로 변경했습니다.
- 무료 인스턴스와 세션 안정성을 고려해 worker를 1개로 설정했습니다.
- Build 단계에서 pip check, Flask app import, gunicorn config 검사를 수행하도록 변경했습니다.
- /healthz 상태 확인 경로를 유지했습니다.
- PYTHON_VERSION=3.13.5 및 SECRET_KEY 자동 생성 설정을 유지했습니다.

3. 로컬 CMD 실행 편의
- run_local.cmd를 추가했습니다.
- 더블클릭 또는 CMD에서 run_local.cmd 실행 시 .venv 확인 -> 필요한 패키지 확인/설치 -> 브라우저 열기 -> Flask 실행 순서로 동작합니다.

4. 배포 안내 추가
- RENDER_DEPLOY.txt를 추가했습니다.
- GitHub 저장소가 AX_Master_2/scm 구조일 때와 scm 자체가 저장소 루트일 때의 Root Directory 차이를 적었습니다.
- legacy/README.md를 추가해 legacy requirements.txt를 Render에서 사용하지 않도록 표시했습니다.

5. 검증 결과
- Python 문법 검사 통과
- JavaScript 문법 검사 통과
- pytest: 20 passed
- 로컬 Flask 실행 확인: /healthz -> {"status":"ok"}
- 로컬 메인 화면 HTTP 200 확인

중요: 과거 Render 로그에서 Streamlit/NumPy/Pandas 등이 설치되고 gunicorn이 없었다면,
현재 scm/requirements.txt가 아니라 legacy/requirements.txt 또는 예전 커밋/잘못된 Root Directory를 사용한 것입니다.
