# Legacy Streamlit archive

This folder is only an archive of the previous Streamlit implementation.
Do not set Render's Root Directory to `scm/legacy` and do not use
`legacy/requirements.txt` for the current deployment.

Current production entry point: `../app.py` (Flask)
Current production dependencies: `../requirements.txt`
